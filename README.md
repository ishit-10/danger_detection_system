# danger_detection_system
eal-time computer vision prototype that detects a red-colored object (representing a user’s hand) and monitors its proximity to a defined virtual danger zone on screen using OpenCV
